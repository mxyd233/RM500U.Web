<Project>
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <TargetFramework>net10.0</TargetFramework>
    <RuntimeIdentifier>linux-arm64</RuntimeIdentifier>
    <SelfContained>true</SelfContained>
    <PublishAot>true</PublishAot>
    <PublishTrimmed>true</PublishTrimmed>
    <TrimMode>full</TrimMode>
    <Optimize>true</Optimize>
    <OptimizationPreference>Speed</OptimizationPreference>
    <ServerGarbageCollection>true</ServerGarbageCollection>
    <IlcInstructionSet>armv8-a</IlcInstructionSet>
    <!-- Keep Native AOT builds viable on low-memory ARM64 builders. -->
    <IlcSingleThreaded>true</IlcSingleThreaded>
    <PublishDir>$(MSBuildProjectDirectory)/publish/native-aot/linux-arm64/</PublishDir>
  </PropertyGroup>
</Project>
